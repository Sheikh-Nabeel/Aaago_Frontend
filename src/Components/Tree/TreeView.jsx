import React from 'react';
import MLMTree from '../MLM/MLMTree';

// For now, Tree and MLM use the same component
// This can be customized later if needed
const TreeView = () => {
  return <MLMTree />;
};

export default TreeView; 